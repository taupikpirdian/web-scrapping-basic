# ============================================
# Database Configuration
# ============================================
# Pilih tipe database: mysql atau postgresql
DB_TYPE=mysql

# Database Connection
# Untuk Docker dengan network_mode: host, gunakan localhost
# Untuk tanpa Docker, sesuaikan dengan host database Anda
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=myorbit_market_data

# ============================================
# Scraper Configuration
# ============================================
# Daftar emiten dipisahkan dengan koma (case-insensitive)
EMITEN_LIST=bbca,indf,tlkm,unvr

# ============================================
# Docker Notes:
# ============================================
# 1. Environment variables ini akan otomatis diload oleh docker-compose
# 2. Pastikan file .env ada di directory yang sama dengan docker-compose.yml
# 3. Untuk production di server:
#    - DB_HOST bisa diisi "localhost" jika menggunakan network_mode: host
#    - Atau isi dengan IP server jika MySQL di server berbeda
#    - Contoh: DB_HOST=192.168.1.100
